<link rel="stylesheet" type="text/css" href="{{ base }}/assets/css/theme{% if site.debug.dist != false %}.min{% endif %}.css"/>

{%- if extra_scss.size > 0 -%}
    <style type="text/css">{{ extra_scss | scssify | strip_newlines }}</style>
{%- endif -%}
